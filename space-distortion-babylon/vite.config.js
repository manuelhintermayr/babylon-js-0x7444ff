// publicDir points at the sibling Three.js project's public/ so the
// blob/textures are served from a single location without duplication.
export default {
  publicDir: "../space-distortion/public/",
};
