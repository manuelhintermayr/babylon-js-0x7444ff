// publicDir points at the sibling Three.js project's public/ so the
// HDR + texture assets are served from a single location without
// duplication.
export default {
  publicDir: "../grass/public/",
};
