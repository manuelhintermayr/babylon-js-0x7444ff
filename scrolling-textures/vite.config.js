// publicDir points at the sibling Three.js project's public/ so the
// water texture is served from a single location without duplication.
export default {
  base: "./",
  publicDir: "./public/",
};
